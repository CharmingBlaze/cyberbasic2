# Deprecated / moved files

This folder only keeps deprecated manual diagnostics that are not part of the main build or test suite.

- **test_raylib_window.go** – Manual raylib/OpenGL diagnostic; excluded from the main build via `// +build ignore`.

Do not rely on deprecated files for normal development. Prefer `examples/first_game.bas`, `examples/hybrid_update_draw_demo.bas`, and the rest of `examples/` for current API usage.
