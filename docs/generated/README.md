# Generated command inventory

Files in this directory are produced by:

```bash
make foreign-audit
```

which runs [`internal/tools/foreignaudit`](../../internal/tools/foreignaudit). Commit updates whenever bindings change so CI can detect drift.
